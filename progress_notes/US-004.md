## 2026-07-31 21:40 - US-004
- 实现店铺售后规则 API（backend/app/routers/rules.py）：
  - GET /api/v1/rules（可选 store_id 过滤，按 updated_at 倒序，含 store_name）
  - POST /api/v1/rules（store_id 必填且须存在否则 400；rule_type 枚举校验：退款/运费/时效/纠纷/备注，非法 400；返回 201）
  - GET /api/v1/rules/{id}（不存在 404）
  - PUT /api/v1/rules/{id}（部分更新，store_id/rule_type 变更时校验；不存在 404）
  - DELETE /api/v1/rules/{id}（204；不存在 404）
- 验证通过（本机 8012 端口临时起服务）：
  - 列表返回 5 条种子规则且各带 store_name
  - store_id=1 过滤只返回店铺 1 的 2 条规则
  - POST 合法规则 → 201（id=6）；POST store_id=999 → 400 "店铺不存在"；POST rule_type=非法类型 → 400
  - PUT id=6 → 200 且 updated_at 变化；PUT id=999 → 404
  - DELETE id=6 → 204；DELETE id=999 → 404
  - python -m compileall app 无错误
- 测试数据已清理（删除 id=6），数据库回到种子状态
- **Learnings for future iterations:**
  - 端口 8011 也被并行 worker 的测试服务器占用（US-003 测试中）；测试时先探测空闲端口（8012+），不要假设 8011/8010 可用
  - 运行中的 uvicorn 不会热加载新代码，改了路由文件必须新起一个端口验证
  - 代码风格统一：Pydantic 模型定义在路由文件内部（不碰 schemas.py）、辅助函数 _to_out/_check_xxx/_get_xxx_or_404、中文 detail 消息、路由路径用 ""（前缀已含 /api/v1/rules）
  - 并行 worker 场景下不要 git add -A 提交他人文件；只 add 自己负责的文件
---
